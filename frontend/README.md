# Frontend Scaffold

This directory is a scaffold for a future web UI.

Current status:
- Basic Vite + React structure only
- Not connected to the Soroban contract yet

To run later:
1. `cd frontend`
2. `npm install`
3. `npm run dev`
